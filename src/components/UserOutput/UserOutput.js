import React from "react";

const UserOutput = props => {
  return (
    <div className="UserOutput">
      <p>{props.username}</p>
      <p>Foo Bar</p>
      <p>Foo Bar</p>
    </div>
  );
};

export default UserOutput;
